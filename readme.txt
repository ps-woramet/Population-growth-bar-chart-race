0. install project
    npm init
    npm install
    npm i express
    npm i nodemon
    npm i cors
    npm i csv-parser
    npm i body-parser
    npm i dotenv --save

1. fix package.json

    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node index",
        "dev": "nodemon index"
    }

1. create index.js

2. index.html


